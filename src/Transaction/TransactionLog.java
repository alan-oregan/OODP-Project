package Transaction;

/*
 * TransactionLog interface to get the transaction log for the transaction
 * base class for TransactionLogDecorator
 */
public interface TransactionLog {
    String getTransactionLog(); // get the transaction log
}
